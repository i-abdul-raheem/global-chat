export const ChatRight = ({ children }) => {
  return (
    <div
      style={{
        height: "100vh",
        background: "#07325d",
        position: "relative",
      }}
      className="col-9 p-0"
    >
      {children}
    </div>
  );
};
